package com.training.model;

public class DiscountCalculator {
	public double calculateDiscount(Discount discount, double amount) {
		double result = discount.calculateDiscount(amount);
		return result;
	}
}
